# FinSight AI — Python dependencies
# Verified against current implementation (src/ and tests/).
# Python 3.12 compatible.
#
# Install:  pip install -r requirements.txt
#
# Sections:
#   1. Core runtime (backend + ML)
#   2. Frontend
#   3. Development / testing
#   4. Scripts (PDF fixture generation)

# ---------------------------------------------------------------------------
# 1. Core runtime
# ---------------------------------------------------------------------------

# FastAPI and ASGI server
fastapi==0.110.0
uvicorn>=0.27.1
python-multipart>=0.0.9   # required by FastAPI for Form/File uploads
pydantic>=2.6.3

# HTTP client used by the Streamlit → API layer
requests>=2.31.0

# Configuration
python-dotenv>=1.0.1

# Data / ML
pandas>=2.2.0
numpy>=1.26.4
scikit-learn>=1.4.0        # Categorizer (TF-IDF + LR) + AnomalyDetector (IsolationForest)
joblib>=1.3.0              # Categorizer model save/load (sklearn dependency, explicit)

# PDF parsing — src/ingestion/pdf_parser.py
pdfplumber==0.10.4         # primary table + text extraction; 0.11.x breaks password-PDF handling
pdfminer.six>=20221105     # provides PDFPasswordIncorrect exception imported directly

# LLM agent — src/agent/agent.py
groq>=0.5.0

# Vector store — src/api/vector_store.py (lazy-loaded, but declared as runtime dep)
sentence-transformers>=2.7.0

# Synthetic data — src/ingestion/synthetic_generator.py
faker>=24.0.0

# ---------------------------------------------------------------------------
# 2. Frontend
# ---------------------------------------------------------------------------

streamlit>=1.35.0

# ---------------------------------------------------------------------------
# 3. Development / testing
# ---------------------------------------------------------------------------

pytest>=8.0.2
pytest-mock>=3.12.0        # MagicMock / patch fixtures in unit tests
hypothesis>=6.100.0        # property-based tests in tests/frontend/

# ---------------------------------------------------------------------------
# 4. Scripts only (PDF fixture generation in scripts/)
# ---------------------------------------------------------------------------

reportlab>=4.1.0           # scripts/generate_pdf_fixtures.py
pypdf>=4.1.0               # scripts/generate_pdf_fixtures.py
